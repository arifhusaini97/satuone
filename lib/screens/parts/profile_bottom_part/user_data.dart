import 'package:flutter/material.dart';
import 'package:satuone/screens/items/neumorphic_figures/skills_category.dart';

class UserData extends StatelessWidget {
  UserData();

  @override
  Widget build(BuildContext context) {
    return Container(child: SkillsCategory());
  }
}
